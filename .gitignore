# dependencies (regenerate with: cd test && npm install)
node_modules/
test/node_modules/

# npm debug output
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# C0fi runtime artifacts (exported apps / flows usually land in ~/Downloads,
# but ignore them here in case anyone builds inside the repo)
*.log

# superseded versions live in archive/ (kept locally, not published) — only the
# current c0fi-v*.html and its guide stay in the repo root
archive/
